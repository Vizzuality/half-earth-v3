import React from 'react'
import TopMenuComponent from './top-menu-component';

function TopMenuContainer(props) {
  return (
    <TopMenuComponent {...props}/>
  )
}

export default TopMenuContainer;
