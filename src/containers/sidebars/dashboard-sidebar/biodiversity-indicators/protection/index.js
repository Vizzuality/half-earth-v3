import React from 'react';

import ProtectionComponent from './protection-component';

function ProtectionContainer(props) {
  return <ProtectionComponent {...props} />;
}

export default ProtectionContainer;
