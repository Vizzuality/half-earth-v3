import { createAction } from 'redux-tools';

export const setAreaTypeSelected = createAction(
  'SET_AREA_TYPE_SELECTED',
);

export const setSidebarTabActive = createAction(
  'SET_SIDEBAR_TAB_ACTIVE',
);
